pipeline {
  agent any
  stages {
    stage('Pull Changes') {

      steps {
         echo 'clone the repo'
         sh 'echo stpe 1'
      }
       steps {
         sh 'echo step 2'
            }
    }

  }
}
