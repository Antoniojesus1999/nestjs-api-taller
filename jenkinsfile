pipeline {
  agent any
    stage('Pull Changes') {

      steps {
         echo 'clone the repo'
         sh 'echo stpe 1'
         sh 'ls -la'
      }
       steps {
         sh 'echo step 2'
            }

    }

}
